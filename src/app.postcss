@tailwind base;
@tailwind components;
@tailwind utilities;
@tailwind variants;

html,
body {
	@apply overflow-hidden;
}
a.inline-link {
	@apply underline font-bold;
}
a.stand-alone-link {
	@apply font-bold text-xl underline;
}
.page-one-col {
	@apply flex flex-col gap-2 mx-auto container p-2;
}

.page-section {
	@apply flex flex-col gap-2 p-2;
}

h2.page-header {
	@apply font-bold text-3xl sm:text-4xl sm:leading-3xl leading-4xl py-1;
}
.page-section h2 {
	@apply font-bold text-2xl sm:text-3xl sm:leading-4xl leading-3xl py-1;
}
.page-section h3 {
	@apply font-bold text-xl sm:text-2xl sm:leading-2xl leading-3xl py-1;
}
.page-section h4 {
	@apply text-xl sm:text-2xl sm:leading-2xl leading-3xl py-1;
}

blockquote {
	@apply pl-4;
}

blockquote:before {
	content: ' " ';
	display: inline;
	height: 0;
	line-height: 0;
	left: -10px;
	position: relative;
	top: 30px;
	color: #ccc;
	font-size: 3em;
}
